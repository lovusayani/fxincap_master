# Copy to .env.production for local production-like builds (gitignored).
# Live server: set VITE_API_URL in repo-root `.deploy.env` (see .deploy.env.example); install-prod.sh exports it for `pnpm build`.
#
# Public REST API (fxincapapi) — no trailing slash.
VITE_API_URL=https://api.ncapfx.com

VITE_APP_URL=https://trade.ncapfx.com
VITE_APP_NAME=SUIMFX Forex Trading Platform
