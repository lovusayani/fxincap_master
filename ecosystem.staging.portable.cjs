const path = require("path");

const baseDir = __dirname;
const logsDir = path.join(baseDir, "logs");

module.exports = {
  apps: [
    {
      name: "fxincap-app",
      cwd: path.join(baseDir, "fxincap"),
      script: "pnpm",
      args: "start",
      instances: 1,
      exec_mode: "fork",
      env: {
        NODE_ENV: "staging",
        PORT: 4000,
      },
      error_file: path.join(logsDir, "fxincap-error.log"),
      out_file: path.join(logsDir, "fxincap-out.log"),
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      autorestart: true,
      watch: false,
      max_memory_restart: "500M",
    },
    {
      name: "fxincap-admin",
      cwd: path.join(baseDir, "fxincapadmin"),
      script: "pnpm",
      args: "run start",
      instances: 1,
      exec_mode: "fork",
      env: {
        NODE_ENV: "staging",
        PORT: 5001,
      },
      error_file: path.join(logsDir, "fxincapadmin-error.log"),
      out_file: path.join(logsDir, "fxincapadmin-out.log"),
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      autorestart: true,
      watch: false,
      max_memory_restart: "500M",
    },
    {
      name: "fxincap-api",
      cwd: path.join(baseDir, "fxincapapi"),
      script: "node",
      args: "dist/index.js",
      instances: 1,
      exec_mode: "fork",
      env: {
        NODE_ENV: "staging",
        PORT: 7000,
      },
      error_file: path.join(logsDir, "fxincapapi-error.log"),
      out_file: path.join(logsDir, "fxincapapi-out.log"),
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      autorestart: true,
      watch: false,
      max_memory_restart: "500M",
    },
    {
      name: "fxincap-trade",
      cwd: path.join(baseDir, "fxincaptrade"),
      script: "node",
      args: "dist/server/start.js",
      instances: 1,
      exec_mode: "fork",
      env: {
        NODE_ENV: "staging",
        PORT: 3000,
      },
      error_file: path.join(logsDir, "fxincaptrade-error.log"),
      out_file: path.join(logsDir, "fxincaptrade-out.log"),
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      autorestart: true,
      watch: false,
      max_memory_restart: "500M",
    },
    {
      name: "fxincap-ws",
      cwd: path.join(baseDir, "fxincapws"),
      script: "node",
      args: "src/server.js",
      instances: 1,
      exec_mode: "fork",
      env: {
        NODE_ENV: "staging",
        WS_PORT: 4040,
      },
      error_file: path.join(logsDir, "fxincapws-error.log"),
      out_file: path.join(logsDir, "fxincapws-out.log"),
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      autorestart: true,
      watch: false,
      max_memory_restart: "500M",
    },
  ],
};